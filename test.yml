---

- hosts: 'webservers'
  become: true
  tasks:
    - name: '{{ first_task_name }}'
      apt:
        name:
          - nginx
          - mariadb-server
          - mariadb-client
          - php-fpm
          - php-mysql
        state: 'present'
      become: true
